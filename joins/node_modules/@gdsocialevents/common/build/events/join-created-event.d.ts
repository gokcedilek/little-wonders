import { Subjects } from './subjects';
import { JoinStatus } from './types/join-status';
export interface JoinCreatedEvent {
    subject: Subjects.JoinCreated;
    data: {
        id: string;
        version: number;
        status: JoinStatus;
        user: {
            id: string;
            email: string;
        };
        post: {
            id: string;
        };
    };
}
