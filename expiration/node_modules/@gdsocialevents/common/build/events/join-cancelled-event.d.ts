import { Subjects } from './subjects';
export interface JoinCancelledEvent {
    subject: Subjects.JoinCancelled;
    data: {
        id: string;
        version: number;
        post: {
            id: string;
        };
    };
}
