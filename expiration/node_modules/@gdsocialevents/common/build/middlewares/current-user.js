"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var jsonwebtoken_1 = __importDefault(require("jsonwebtoken"));
//declare a user as "logged in"
exports.currentUser = function (req, res, next) {
    //if the user is logged in, extract user info from the jwt and set req.currentUser
    //console.error('inside currUser mware, printing req.session.jwt: ');
    if (!req.session || !req.session.jwt) {
        //console.error('hit1');
        return next();
    }
    try {
        var payload = jsonwebtoken_1.default.verify(req.session.jwt, process.env.JWT_KEY); //define a type for payload
        req.currentUser = payload;
        //console.error('hit2');
        //console.error(req.currentUser);
    }
    catch (err) {
        //console.error('hit3');
    }
    next();
};
