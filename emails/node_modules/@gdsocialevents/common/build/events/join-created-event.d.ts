import { Subjects } from './subjects';
export interface JoinCreatedEvent {
    subject: Subjects.JoinCreated;
    data: {
        id: string;
        version: number;
        userId: string;
        post: {
            id: string;
        };
    };
}
