import { Subjects } from './subjects';
export interface PostUpdatedEvent {
    subject: Subjects.PostUpdated;
    data: {
        id: string;
        version: number;
        title: string;
        description: string;
        userId: string;
        numPeople: number;
        location: string;
        time: string;
        joinIds?: [string];
    };
}
