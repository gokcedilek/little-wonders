"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var CustomError_1 = require("./CustomError");
//middleware that determines what kind of error we received
//see notes/errorHandling.txt for explanation of this function
exports.errorHandler = function (err, req, res, next) {
    //send a consistent error structure to the frontend of our app
    if (err instanceof CustomError_1.CustomError) {
        console.error('custom error');
        res.status(err.statusCode).send({ errors: err.serializeErrors() });
    }
    else {
        console.log('not a custom error');
        res.status(400).send({
            errors: [
                {
                    message: 'Something went wrong!',
                },
            ],
        });
    }
};
